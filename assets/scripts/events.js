'use strict'

const getFormFields = require('../../lib/get-form-fields')
const gameboardRef = require('./gameboard')

const toPlayerArray = function (event) {
  event.preventDefault()
  const data = this
}
module.exports = {
  toPlayerArray
}
