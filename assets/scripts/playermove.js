'use strict'

const playerOneMove = function (event) {
  playerarr.push({
    'move': currentMove
  })
}

const playerTwoMove = function (event) {
  playerarr.push({
    'move': events.currentMove
  })
}

module.exports = {
  playerOneMove,
  playerTwoMove
}
